meta {
  name: create_deployment
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/v2/lm/deployments
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{bearerToken}}
  AI-Resource-Group: {{resourceGroup}}
  Content-Type: application/json
}

body:json {
  {
    "configurationId": "{{configurationId}}"
  }
}

